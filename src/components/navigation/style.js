const styles = ({ palette, typography }) => ({
  container: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-between',
    listStyle: 'none',
    position: 'relative',
    zIndex: 10,
    margin: 0
  },
});

export default styles;
