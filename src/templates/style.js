const styles = ({ typography }) => ({
    container: {
        padding: `${typography.pxToRem(48)} ${typography.pxToRem(4)} `,
        border: '2px',
        borderColor: '#707070'
    },
    gridList: {
    width: 500,
    height: 450,
  }
});

export default styles;
