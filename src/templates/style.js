const styles = ({ typography }) => ({
    container: {
        padding: `${typography.pxToRem(48)} ${typography.pxToRem(4)} `,
        border: '2px',
        borderColor: '#707070',
    }
});

export default styles;
